InforiaConnect Revision Log
===========================

Date: 2026-06-05
Title: Stabilize Rust dependency resolution and identify CI builds
Intended GitHub Actions run name:
  Stabilize Rust dependencies and name Windows CI builds

Changed files:
  - .github/workflows/windows-build.yml
  - Cargo.lock
  - AGENTS.md
  - logging/revisions.txt

Changes:
  - Changed the active Windows workflow Rust toolchain from version 1.75 to
    stable for both bridge generation and Windows compilation.
  - Added a required build_name input for manually dispatched workflow runs.
  - Added a workflow-level run-name so builds are identifiable in the GitHub
    Actions tab. Push-triggered builds use the descriptive commit message.
  - Generated Cargo.lock to pin registry versions and Git dependency commits.
  - Added repository operating rules covering GitHub-only authoritative builds,
    independent diagnosis, installer and server-configuration requirements,
    revision logging, validation, commit ordering, and pushing to GitHub.

Reasons:
  - Cargo 1.75 cannot parse dependencies that use Rust Edition 2024.
  - Cargo 1.81 also fails on the resolved time 0.3.47 manifest because Edition
    2024 support was not stable in that Cargo release.
  - Using the stable CI toolchain allows the current dependency graph to be
    parsed, while Cargo.lock prevents future builds from silently resolving a
    different graph.
  - Named runs make each compilation attempt and tested hypothesis visible in
    GitHub Actions.
  - A permanent repository-local process is needed so every future change is
    documented before it is committed and pushed.

Validation:
  - Confirmed no Rust 1.75 override remains in the active Windows workflow or
    root Cargo.toml.
  - Generated Cargo.lock successfully.
  - Confirmed tray-icon is locked to registry version 0.21.3.
  - Confirmed stable Cargo 1.96.0 parses the locked metadata.
  - Confirmed stable Cargo fetched and parsed the complete locked Windows
    dependency set.
  - A full Windows Flutter/vcpkg installer build is intentionally delegated to
    GitHub Actions, the authoritative compilation environment.

-------------------------------------------------------------------------------

Date: 2026-06-05
Title: Split Windows builds into baseline and accelerated profiles
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Add baseline and accelerated CI profiles

Changed files:
  - .github/workflows/windows-build.yml
  - AGENTS.md
  - logging/revisions.txt

Changes:
  - Added a required build_profile choice to manual workflow runs with
    `baseline` and `accelerated` options.
  - Made `baseline` the automatic profile for every push to master or main.
  - Added the selected build profile to the GitHub Actions run name and
    uploaded artifact name.
  - Split native dependency installation by profile.
  - Baseline installs only the software codec dependencies required by the
    RustDesk core: aom, libjpeg-turbo, libvpx, libyuv, and opus.
  - Baseline forces vcpkg classic mode so the explicit package list is used
    instead of the repository manifest that includes FFmpeg.
  - Baseline builds with `python3 build.py --portable --flutter`.
  - Accelerated builds retain the full vcpkg manifest, FFmpeg preparation,
    `--hwcodec`, and `--vram` behavior.
  - Isolated Rust caches by build profile.
  - Documented baseline as the release-validation path and accelerated as an
    independently maintained performance variant.

Reasons:
  - Branding, custom ID/relay server configuration, the public key, remote
    control, and installation do not require hardware codecs or VRAM.
  - Software VP8, VP9, and AV1 support still requires its smaller native codec
    dependency set.
  - The full FFmpeg installation added approximately 51 minutes to the latest
    workflow and the accelerated linker path caused the current
    `swresample.lib` failure.
  - A reliable installer must be deliverable without being blocked by optional
    GPU performance features.
  - Keeping acceleration as a separate selectable profile preserves a clear
    path to optimize multi-monitor 1080p and 1440p sessions later.

Validation:
  - Confirmed the baseline path does not enable the `hwcodec` or `vram` Cargo
    features.
  - Confirmed the accelerated path retains the existing FFmpeg and VRAM setup.
  - Confirmed push events resolve to the baseline profile.
  - Confirmed manual runs require both a descriptive build name and an explicit
    profile selection.
  - Confirmed profile-specific artifact and cache names prevent ambiguity.
  - Full compilation and installer generation are delegated to GitHub Actions.

-------------------------------------------------------------------------------

Date: 2026-06-05
Title: Align baseline vcpkg installation with native build scripts
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Add baseline and accelerated CI profiles
  Run ID: 27019690424
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix baseline vcpkg dependency path

Changed files:
  - .github/workflows/windows-build.yml
  - logging/revisions.txt

Failure:
  - The baseline dependency installation completed successfully.
  - The Rust build then failed in the magnum-opus build script because
    `opus/opus_multistream.h` could not be found.
  - The build script searched under
    `C:\vcpkg\installed\x64-windows-static\include`.
  - The workflow had installed packages under the workspace-specific
    `vcpkg_installed` directory instead.

Changes:
  - Removed the baseline `--x-install-root` override so vcpkg installs the
    software codec packages under `C:\vcpkg\installed`.
  - Removed `VCPKG_INSTALLED_ROOT` from the baseline build environment.
  - Retained `VCPKG_ROOT=C:\vcpkg`, which is the location expected by both
    magnum-opus and scrap native build scripts.

Reasons:
  - magnum-opus does not honor the workflow's workspace installation override
    in this dependency version and derives its include path from VCPKG_ROOT.
  - Using vcpkg's standard installed directory gives all native build scripts
    one consistent source for Opus, libvpx, AOM, and libyuv.
  - No package or application feature change is required; this is only a path
    consistency correction.

Validation:
  - Confirmed the failed log resolved Opus from
    `C:\vcpkg\installed\x64-windows-static`.
  - Confirmed the revised install and build steps both use `C:\vcpkg`.
  - Confirmed baseline still excludes FFmpeg, hwcodec, and vram.
  - Full compilation and installer generation are delegated to GitHub Actions.

-------------------------------------------------------------------------------

Date: 2026-06-05
Title: Resolve the portable packer from the Cargo workspace target
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Fix baseline vcpkg dependency path
  Run ID: 27021453379
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix portable packer workspace path

Changed files:
  - build.py
  - logging/revisions.txt

Failure:
  - The baseline native dependencies, Rust core, Flutter application, and
    portable payload generation completed successfully.
  - `libs/portable/generate.py` generated `data.bin` and Cargo compiled
    `InforiaConnect-portable-packer` successfully.
  - Packaging then failed because `build.py` searched for the executable under
    `libs/portable/target/release`.

Changes:
  - Updated both supported portable packer source paths to resolve from the
    repository workspace target directory at `../../target/release`.
  - Clarified the missing-binary error message to identify the workspace target
    directory.

Reasons:
  - `libs/portable` is a member of the root Cargo workspace.
  - Cargo therefore places its release executable in the root
    `target/release` directory, not a crate-local `libs/portable/target`.
  - The packer existed after compilation, but the previous lookup path could
    never find it.

Validation:
  - Confirmed `libs/portable` is listed in the root Cargo workspace members.
  - Confirmed the portable package binary name is
    `InforiaConnect-portable-packer`.
  - Confirmed the corrected relative path resolves from `libs/portable` to the
    root workspace `target/release` directory.
  - Full packaging and installer generation are delegated to GitHub Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T17:30:52+02:00
Title: Add a dedicated timestamped build-error ledger
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Add timestamped build error ledger

Changed files:
  - AGENTS.md
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Created `logging/BuildError_log.txt` as a chronological record of confirmed
    CI and packaging errors.
  - Reconstructed the verified junction, Edition 2024, FFmpeg linker, Opus
    include-path, and portable packer failures from Actions logs and Git
    history.
  - Recorded detection and fix timestamps, run IDs, root causes, attempted or
    completed fixes, commits, statuses, and validation outcomes.
  - Added permanent agent rules requiring every future confirmed build error
    and fix to be timestamped in the dedicated log.
  - Required failed attempts to remain in the history rather than being
    overwritten after a later fix.

Reasons:
  - `revisions.txt` records repository changes, but a separate failure ledger
    makes the build investigation sequence and repeated errors easier to audit.
  - Precise timestamps and run IDs connect each diagnosis to authoritative
    GitHub Actions evidence.
  - Preserving unsuccessful attempts prevents the same ineffective fix from
    being repeated.

Validation:
  - Cross-checked run IDs and failure timestamps with GitHub Actions.
  - Cross-checked fix timestamps and commit hashes with Git history.
  - Confirmed the new rules require updates before future commits and pushes.
  - The active baseline build remains run 27023798667.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T17:35:29+02:00
Title: Simplify the build-error log
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Simplify build error log

Changed files:
  - AGENTS.md
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Condensed each build-error entry to its timestamp, error summary, attempted
    fix, and outcome.
  - Removed run IDs, commit hashes, detailed root-cause sections, and lengthy
    diagnostic descriptions from the dedicated error log.
  - Updated the permanent agent rules to prohibit copying full Actions logs or
    lengthy diagnostic output into `BuildError_log.txt`.

Reasons:
  - The dedicated file is intended as a quick human-readable troubleshooting
    summary, while detailed repository changes remain in `revisions.txt`.
  - GitHub Actions remains the source for complete raw build output.

Validation:
  - Confirmed every existing entry uses the requested concise format.
  - Confirmed timestamps and outcomes were preserved.
  - Confirmed no build or application code was changed.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T18:17:31+02:00
Title: Make the packaging success message Windows-console safe
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Simplify build error log
  Run ID: 27024383876
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix packaging console encoding

Changed files:
  - build.py
  - logging/BuildError_log.txt
  - logging/revisions.txt

Failure:
  - The Rust core, Flutter application, portable payload, and portable packer
    compiled successfully.
  - `data.bin` and app metadata were generated successfully.
  - `build.py` moved the packer to the final
    `InforiaConnect-<version>-install.exe` filename.
  - The script then raised UnicodeEncodeError because the Windows CP1252
    console could not encode the sparkle character in its success message.
  - The nonzero exit prevented the artifact preparation and upload steps.

Changes:
  - Replaced the Unicode sparkle with a plain ASCII success message.
  - Marked the preceding portable-packer path error as fixed in the concise
    build-error log.
  - Added a concise entry for this console-encoding failure and attempted fix.

Reasons:
  - Status output must not make an otherwise successful package build fail.
  - ASCII output is portable across the GitHub Windows runner's console
    encodings.

Validation:
  - Confirmed the failing line was executed after the final installer rename.
  - Confirmed no other sparkle character remains in `build.py`.
  - Confirmed the workflow artifact step expects the generated
    `InforiaConnect-*-install.exe` pattern.
  - Full artifact preparation and upload are delegated to GitHub Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T19:00:45+02:00
Title: Verify the successful baseline Windows artifact
Successful GitHub Actions run:
  InforiaConnect Windows [baseline] - Fix packaging console encoding
  Run ID: 27026509631
  Commit: 47f508b938f6914346cb2e85a5d7d608af38ce79

Changed files:
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Marked the Windows console-encoding failure as fixed.
  - Recorded the successful build and downloaded-artifact verification.

Reasons:
  - The final outcome must be preserved after GitHub performs the authoritative
    build and artifact upload.

Validation:
  - GitHub Actions completed both `generate-bridge` and `build-windows`
    successfully.
  - The baseline build, artifact preparation, and artifact upload steps all
    completed successfully.
  - Artifact name: `InforiaConnect-Windows-baseline-Setup`.
  - Artifact ID: 7442142048.
  - Artifact archive size: 21,829,483 bytes.
  - Artifact archive SHA-256:
    `249f7884fa55ce5b85ad1242e1373f863ee2ec54da31d32ebf02c50873398ded`.
  - Downloaded executable: `InforiaConnect-Windows-Setup.exe`.
  - Executable size: 22,175,232 bytes.
  - Executable SHA-256:
    `273A2439FB2E9C0A06B789D25022C8B327A8253819B2C2D3C34674799204EEC8`.
  - Confirmed the executable begins with the Windows PE `MZ` header.
  - Confirmed product metadata identifies InforiaConnect version 1.4.6.
  - The current output is RustDesk's portable packed executable renamed to
    `Setup.exe`; a traditional installation wizard was not built or executed.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T19:48:29+02:00
Title: Fix the portable package marker and add runtime validation
Failed runtime artifact:
  GitHub Actions run ID: 27026509631
  Artifact ID: 7442142048
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix silent portable launcher crash

Changed files:
  - .github/workflows/windows-build.yml
  - libs/portable/src/bin_reader.rs
  - libs/portable/src/main.rs
  - logging/BuildError_log.txt
  - logging/revisions.txt

Failure:
  - The GitHub workflow compiled and uploaded
    `InforiaConnect-Windows-Setup.exe` successfully.
  - Double-clicking the executable displayed nothing.
  - Local reproduction with `--version` exited immediately with Windows status
    `0xC0000409` and did not extract `InforiaConnect.exe`.
  - The package generator wrote `InforiaConnect` as its start and end marker.
  - The Rust package reader retained the original RustDesk marker length of
    eight bytes, so its first marker comparison always failed and panicked.
  - The Windows GUI subsystem hid the panic message from the user.

Changes:
  - Replaced the hard-coded eight-byte marker length with the complete
    `InforiaConnect` byte marker.
  - Updated both start-marker and end-marker parsing to use the marker's actual
    length.
  - Made the portable launcher recognize both `install.exe` and `setup.exe`
    filename suffixes as installation mode.
  - Added focused tests for installer filename detection.
  - Added a GitHub Actions smoke test after artifact preparation.
  - The smoke-test gate runs the portable launcher's focused Rust tests.
  - The smoke test launches the package with `--version`, requires a successful
    exit within 30 seconds, and verifies that the embedded
    `InforiaConnect.exe` was extracted under LocalAppData.
  - The smoke test also launches the extracted application with `--version`
    and requires a successful exit.
  - Artifact upload now occurs only after this runtime smoke test passes.

Reasons:
  - Compilation alone cannot prove that the self-extracting launcher can parse
    its embedded payload.
  - Using the marker value's length prevents branding changes from leaving a
    stale RustDesk-specific length in the parser.
  - Runtime validation prevents another silently crashing executable from
    being published as a successful artifact.
  - The uploaded artifact is named `Setup.exe`, so installation-mode detection
    must support that suffix rather than only `install.exe`.

Validation:
  - Confirmed the downloaded artifact reproduces the immediate exit and no
    extraction.
  - Confirmed the generator writes the 14-byte `InforiaConnect` marker.
  - Confirmed the previous reader consumed only eight bytes before comparing
    against `InforiaConnect`.
  - Confirmed `--version` is a non-installing application command suitable for
    the package smoke test.
  - Confirmed the workflow publishes the package as
    `InforiaConnect-Windows-Setup.exe`.
  - Full Windows compilation and runtime smoke validation are delegated to
    GitHub Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T20:26:26+02:00
Title: Align Windows runtime DLL loading with the branded Rust library
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Fix silent portable launcher crash
  Run ID: 27031154852
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix branded runtime DLL loading

Changed files:
  - flutter/lib/models/native_model.dart
  - flutter/windows/runner/main.cpp
  - logging/BuildError_log.txt
  - logging/revisions.txt

Failure:
  - The portable launcher's installer-name unit test passed.
  - The packaged executable returned exit code 0 and extracted
    `InforiaConnect.exe`, proving the package marker fix worked.
  - Starting the extracted application returned exit code 1.
  - The Windows runner still called `LoadLibraryA("librustdesk.dll")`.
  - Cargo names the custom Rust library `libInforiaConnect`, CMake packages
    `libInforiaConnect.dll`, and the extracted payload contains that filename.
  - The Dart native library loader also retained `librustdesk.dll`.

Changes:
  - Updated the native Windows runner to load `libInforiaConnect.dll`.
  - Updated its diagnostic message to report the branded DLL filename.
  - Updated the Dart Windows FFI loader to open `libInforiaConnect.dll`.
  - Preserved the runtime smoke gate that starts the extracted application and
    rejects nonzero exit codes.

Reasons:
  - The executable cannot initialize the Rust core when it requests a DLL that
    is not present in its distribution directory.
  - Both native and Dart loaders must match the library name produced by Cargo
    and installed by CMake.

Validation:
  - Confirmed Cargo library name is `libInforiaConnect`.
  - Confirmed Windows CMake packaging installs `libInforiaConnect.dll`.
  - Confirmed the previous artifact payload contained
    `libInforiaConnect.dll`, not `librustdesk.dll`.
  - Confirmed no Windows runtime reference to `librustdesk.dll` remains in the
    Flutter source.
  - Full build, extraction, and startup validation are delegated to GitHub
    Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T21:03:03+02:00
Title: Validate the real setup launch instead of a duplicate application
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Fix branded runtime DLL loading
  Run ID: 27032808662
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Validate setup installation window

Changed files:
  - .github/workflows/windows-build.yml
  - logging/BuildError_log.txt
  - logging/revisions.txt

Failure:
  - Portable launcher tests passed.
  - Package parsing, extraction, and the package process itself succeeded.
  - The smoke test then started `InforiaConnect.exe` a second time.
  - That duplicate process returned exit code 1, so the workflow rejected the
    build without testing the original child process launched by the package.

Changes:
  - Changed the smoke test to launch
    `InforiaConnect-Windows-Setup.exe` without arguments, matching a user
    double-click.
  - Retained checks for package exit code and extracted
    `InforiaConnect.exe`.
  - Added an explicit check for extracted `libInforiaConnect.dll`.
  - Polls for the first embedded `InforiaConnect` process started by the
    package.
  - Requires that process to create a real top-level window within 30 seconds.
  - Reports the opened window title and then stops the process to clean up the
    CI runner.

Reasons:
  - The package is responsible for launching the embedded application, so
    starting another copy does not model user behavior.
  - The user-visible requirement is that double-clicking Setup opens the
    installation UI, not that a second process accepts a CLI command.
  - Checking a nonzero window handle directly guards against another
    build that extracts files but appears to do nothing.

Validation:
  - Confirmed the portable launcher forwards `--install` when a no-argument
    executable name ends in `Setup.exe`.
  - Confirmed the Flutter Windows runner allows `--install` and creates an
    install-page window for that argument.
  - Full UI startup validation is delegated to GitHub Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T21:38:02+02:00
Title: Align Windows runner symbol lookup with branded Rust exports
Failed GitHub Actions run:
  InforiaConnect Windows [baseline] - Validate setup installation window
  Run ID: 27034554116
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix branded Rust symbol lookup

Changed files:
  - flutter/windows/runner/main.cpp
  - logging/BuildError_log.txt
  - logging/revisions.txt

Failure:
  - Package tests, extraction, package exit code, embedded executable presence,
    and branded DLL presence all passed.
  - No InforiaConnect process created a window within 30 seconds.
  - Inspection showed the Windows runner requested
    `rustdesk_core_main_args`, which is not exported by the branded Rust DLL.
  - The Rust DLL exports `InforiaConnect_core_main_args`.
  - The runner also requested `get_rustdesk_app_name`, while the DLL exports
    `get_InforiaConnect_app_name`.
  - This disproved the previous duplicate-instance hypothesis; that attempted
    smoke-test adjustment is preserved in the history.

Changes:
  - Updated core-main symbol lookup to
    `InforiaConnect_core_main_args`.
  - Updated application-name symbol lookup to
    `get_InforiaConnect_app_name`.
  - Changed the fallback Windows title from RustDesk to InforiaConnect.
  - Updated the startup diagnostic message for the core-main symbol.
  - Retained the no-argument setup launch and visible-window runtime gate.

Reasons:
  - `GetProcAddress` requires the exact exported symbol name.
  - The runner cannot initialize the Rust core or proceed to Flutter when the
    core-main lookup returns null.

Validation:
  - Confirmed the two branded functions are exported with `#[no_mangle]` in
    `src/flutter.rs`.
  - Confirmed no definition of `rustdesk_core_main_args` exists in the current
    Rust source.
  - Confirmed `free_c_args`, the third required runner symbol, remains exported
    under the name already used by the runner.
  - Full compilation and visible installation-window validation are delegated
    to GitHub Actions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T22:15:10+02:00
Title: Apply the InforiaConnect runtime name to the installer UI
Successful startup GitHub Actions run:
  InforiaConnect Windows [baseline] - Fix branded Rust symbol lookup
  Run ID: 27036193036
Intended GitHub Actions run name:
  InforiaConnect Windows [baseline] - Fix installer runtime branding

Changed files:
  - .github/workflows/windows-build.yml
  - libs/hbb_common/src/config.rs
  - logging/BuildError_log.txt
  - logging/revisions.txt

Observed issue:
  - Package parsing, extraction, branded DLL loading, Rust symbol lookup, and
    Flutter window creation all succeeded.
  - The runtime smoke test opened `RustDesk - Install`.
  - `hbb_common::config::APP_NAME` was still initialized to `RustDesk`, and the
    exported application-name function returned that value to the runner.

Changes:
  - Changed the authoritative shared runtime application name to
    `InforiaConnect`.
  - Strengthened the setup smoke test to require the exact window title
    `InforiaConnect - Install`.
  - Artifact upload now rejects an installer that opens with upstream RustDesk
    branding.

Reasons:
  - Application title, configuration paths, service naming, and installation
    UI derive from the shared runtime application name.
  - A technically working RustDesk-branded window does not satisfy the
    InforiaConnect custom-client requirement.

Validation:
  - Confirmed run 27036193036 opened a real install window.
  - Confirmed the observed title was `RustDesk - Install`.
  - Confirmed `get_InforiaConnect_app_name` returns the shared APP_NAME value.
  - Confirmed the new workflow assertion requires the branded install title.
  - Full branded runtime validation is delegated to GitHub Actions.
