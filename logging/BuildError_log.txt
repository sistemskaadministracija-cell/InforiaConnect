InforiaConnect Build Error Log
==============================

This file contains short summaries of build errors and the fixes attempted.
Timestamps use Europe/Ljubljana local time.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T11:34:50+02:00
Error:
  The workflow could not create the vcpkg directory junction because its
  destination directory did not exist.
Fix attempted:
  Created the required directory before creating the junction.
Outcome:
  Fixed. The following build passed this step.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T12:39:40+02:00
Error:
  Cargo 1.75 could not read dependencies that require Rust Edition 2024.
Fix attempted:
  First raised the Rust version to 1.81. That did not affect the active
  workflow toolchain, so the workflow was then changed to stable Rust and a
  new Cargo.lock was committed.
Outcome:
  Fixed. Cargo passed dependency parsing in the following build.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T15:35:59+02:00
Error:
  The Windows linker could not find `swresample.lib` because accelerated FFmpeg
  linkage was being forced into the whole build.
Fix attempted:
  Split CI into baseline and accelerated profiles. Baseline excludes FFmpeg,
  hardware codecs, and VRAM features.
Outcome:
  Fixed for the baseline build. The accelerated profile will be handled
  separately after the installer is working.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T16:38:45+02:00
Error:
  The Opus build could not find `opus/opus_multistream.h` because vcpkg
  installed it in a different directory from the one used by the Rust build.
Fix attempted:
  Made baseline vcpkg installation and the Rust build use the standard
  `C:\vcpkg\installed` directory.
Outcome:
  Fixed. The following build compiled Opus and reached packaging.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T17:20:36+02:00
Error:
  Packaging could not find the compiled portable packer executable.
Fix attempted:
  Changed `build.py` to look in the root Cargo workspace `target/release`
  directory instead of `libs/portable/target/release`.
Outcome:
  Fixed. The following build found the packer and created the final installer
  file.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T18:17:31+02:00
Error:
  The installer was created, but `build.py` crashed while printing a sparkle
  character that the Windows CP1252 console could not encode.
Fix attempted:
  Replaced the Unicode sparkle with a plain ASCII success message.
Outcome:
  Fixed. GitHub Actions completed successfully and uploaded the Windows
  executable artifact.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T19:48:29+02:00
Error:
  The uploaded executable exited immediately and displayed nothing because the
  portable reader still expected the old 8-byte RustDesk package marker while
  the generated InforiaConnect marker is 14 bytes.
Fix attempted:
  Changed the reader to use the complete `InforiaConnect` marker, made
  `Setup.exe` names open installation mode, and added runtime smoke tests.
Outcome:
  The marker and setup-name fixes passed. Runtime validation then exposed a
  separate stale DLL name in the extracted application.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T20:26:26+02:00
Error:
  The package extracted correctly, but the embedded application exited with
  code 1 because its Windows loaders requested `librustdesk.dll` while the
  custom build contains `libInforiaConnect.dll`.
Fix attempted:
  Updated the native Windows runner and Dart FFI loader to load the actual
  `libInforiaConnect.dll` filename.
Outcome:
  The DLL correction compiled, but the smoke test incorrectly launched a
  second application instance and treated its rejection as a failure.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T21:03:03+02:00
Error:
  Runtime validation launched the extracted application twice, so the second
  instance exited with code 1 and produced a false failure.
Fix attempted:
  Changed the smoke test to launch `Setup.exe` normally and require the first
  embedded InforiaConnect process to open an installation window.
Outcome:
  This did not fix startup. The next run showed that no application window was
  created, disproving the duplicate-instance explanation.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T21:38:02+02:00
Error:
  The Windows runner loaded the branded DLL but requested old RustDesk export
  names that do not exist in the custom library, so startup stopped before UI.
Fix attempted:
  Updated the runner to request the exported InforiaConnect core-main and
  application-name functions.
Outcome:
  Fixed. The setup window opened successfully, exposing a remaining RustDesk
  title in the runtime configuration.

-------------------------------------------------------------------------------

Timestamp: 2026-06-05T22:15:10+02:00
Error:
  The installer window opened, but its title was `RustDesk - Install` because
  the shared runtime application name was still hard-coded as RustDesk.
Fix attempted:
  Set the runtime application name to InforiaConnect and require the branded
  installer title in the workflow smoke test.
Outcome:
  Fixed. GitHub Actions run 27047079246 built the baseline installer, opened
  the branded installation window in its runtime smoke test, and uploaded the
  final artifact successfully.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T16:13:00+02:00
Error:
  The first GitHub Release command split multiline notes into separate
  PowerShell arguments, and the second attempt used an abbreviated commit SHA
  that GitHub rejected as an invalid release target.
Fix attempted:
  Moved the release notes into a dedicated file and supplied the full pushed
  commit SHA as `target_commitish`.
Outcome:
  Fixed. Release `v1.4.6-inforia.1` was published with the validated Windows
  installer, and its public direct-download endpoint returned HTTP 200.

-------------------------------------------------------------------------------
