InforiaConnect Server Revision Log
==================================

Timestamp: 2026-06-06T00:54:07+02:00
Title: Add reloadable InforiaConnect device ID blocklist

Changes:
  - Created the InforiaConnect-server fork from the stable RustDesk Server
    1.1.15 source.
  - Added a file-backed numerical ID blocklist that reloads automatically.
  - Blocked devices are denied registration, shown as offline, and cannot be
    targeted through punch-hole or relay setup requests.
  - Added helper scripts to block, unblock, and list IDs without committing
    production IDs to Git.
  - Added a Docker Compose deployment definition preserving host networking,
    restart policy, server address, key mode, and persistent storage.
  - Added GitHub Actions compilation, testing, image publication, and smoke
    testing for the custom hbbs/hbbr image.

Reasons:
  - Provide reversible administrative blocking of known InforiaConnect IDs.
  - Keep server changes reproducible and separate from the Windows client.

Validation:
  - Blocklist unit tests passed in GitHub Actions.
  - Static hbbs and hbbr compilation passed.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T03:04:32+02:00
Title: Prevent redundant server and client CI runs

Changes:
  - Excluded client documentation and audit logs from automatic Windows builds.
  - Limited server image builds to files that can change the compiled image.
  - Cancelled redundant workflows started by audit-only commits.

Reasons:
  - Reduce CI time and noise while keeping authoritative builds for all
    implementation and build-pipeline changes.

Validation:
  - The client source build and server image build had already passed.
  - Both workflow definitions retain manual dispatch with descriptive names.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T03:03:15+02:00
Title: Complete VM security audit and container isolation hardening

Changes:
  - Audited OS updates, AppArmor, failed services, login-capable accounts,
    sudo configuration, Docker security options, listeners, SSH events, and
    available malware/rootkit tools.
  - Verified the deployment backup checksum.
  - Recreated hbbs and hbbr with read-only image filesystems, all capabilities
    dropped, and no-new-privileges.

Reasons:
  - Improve production containment using controls appropriate for the minimal
    scratch-based server image without changing network behavior.

Validation:
  - Unattended security upgrades and AppArmor are active.
  - No systemd services are failed and no failed SSH login events were found
    in the seven-day journal window.
  - Backup checksum verification passed.
  - Both hardened containers are running with zero special capabilities and
    all required listeners active.
  - The custom image is immutable and contains only the server binaries.

Remaining security decisions requiring explicit approval:
  - UFW is inactive.
  - SSH password authentication is enabled.
  - The Windows installer is not Authenticode-signed.
  - No antivirus/rootkit scanner is installed. For this minimal container
    workload, patching, AppArmor, immutable images, least privilege, and
    backups are currently the higher-value controls.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:21:15+02:00
Title: Deploy custom hbbs/hbbr and harden persistent data

Changes:
  - Pulled and validated immutable image digest
    `sha256:2a029367efd204ed78f35d8a6e6707cd36c3a4a8dce6af77e1cba173cbd217c7`.
  - Stopped the standard containers and created a consistent compressed backup
    with a SHA-256 checksum.
  - Deployed custom hbbs and hbbr with the original host network, restart
    policy, persistent mount, relay address, and key mode.
  - Added an empty persistent blocked_ids.txt file.
  - Changed the persistent directory to mode 0750 and private key/database
    files to mode 0600.
  - Restarted both containers after permission hardening.

Reasons:
  - Activate server-side ID blocking while preserving production identity and
    reducing local exposure of sensitive server data.

Validation:
  - Backup:
    `/home/inforiaadmin/inforiaconnect-backups/rustdesk-20260606T001915Z.tar.gz`
  - Both containers remained healthy after restart.
  - TCP ports 21115-21119 and UDP 21116 are listening.
  - Public-key checksum remained unchanged.
  - hbbs loaded the database, existing private key, relay address, and zero
    blocked IDs.
  - No UFW, firewall, DNS, routing, port-forwarding, or SSH setting changed.
  - Normal client re-registration and external connection remain acceptance
    tests because no active client registered during the observation window.

Security observations:
  - UFW is inactive.
  - SSH password authentication is enabled.
  - Unattended security upgrades are enabled and active.
  - Network/firewall and SSH changes require separate explicit approval.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:15:00+02:00
Title: Clarify goal-completion scope for VM security work

Changed files:
  - AGENTS.md
  - logging/revisions_server.txt

Changes:
  - Clarified that scope restrictions do not block supporting work genuinely
    required to complete an approved task.
  - Explicitly allowed justified and tested package, service, monitoring,
    malware-protection, and hardening changes when requested by the user.
  - Preserved the distinction between required remediation and unrelated or
    optional improvements.

Reasons:
  - Ensure strict scope control supports complete, secure delivery instead of
    preventing necessary implementation work.

Validation:
  - Reviewed the updated rules for consistency with the existing prohibition
    on unapproved network and firewall changes.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:11:50+02:00
Title: Establish strict VM scope and validation boundaries

Changed files:
  - AGENTS.md
  - logging/revisions_server.txt

Changes:
  - Required post-edit review and practical testing for every implementation.
  - Limited work to explicitly requested tasks and their minimum supporting
    changes.
  - Prohibited unapproved changes to networking, UFW, firewall, DNS, routing,
    port forwarding, SSH configuration, packages, and unrelated services.
  - Defined network and firewall security audits as read-only unless a
    specific change receives separate approval.
  - Required backup, minimal change, validation, and rollback consideration
    before approved VM modifications.

Reasons:
  - Preserve stability and ensure infrastructure access cannot be used beyond
    the user's requested InforiaConnect work.

Validation:
  - Verified dedicated SSH key authentication to 192.168.145.111.
  - Verified the `codex-inforia` account and noninteractive sudo access.
  - Verification used only `id`, `hostname`, and `sudo -n true`; no VM
    configuration or network state was changed.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:06:07+02:00
Title: Define the server image binary PATH

Changes:
  - Added `/usr/bin` to the scratch Docker image PATH.

Reasons:
  - The first image smoke test could not resolve the hbbs command name.

Validation:
  - The next workflow proved that Docker's explicit entrypoint did not perform
    PATH lookup, requiring absolute paths.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:16:40+02:00
Title: Use absolute server binary paths

Changes:
  - Changed workflow smoke tests and Docker Compose commands to use
    `/usr/bin/hbbs` and `/usr/bin/hbbr`.

Reasons:
  - Ensure command resolution works in the minimal scratch image.

Validation:
  - Docker located the binaries successfully.
  - The next smoke test exposed missing executable permissions.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:53:27+02:00
Title: Preserve executable permissions in the server image

Changes:
  - Applied mode 0755 to hbbs and hbbr during Docker COPY.

Reasons:
  - Artifact transport removed executable mode before the scratch image build.

Validation:
  - Server source, blocklist tests, static compilation, image build, and image
    publication passed.
  - GitHub Actions run 27046280606 passed both published-image smoke tests.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:17:05+02:00
Title: Complete custom server image validation and pre-deployment audit

Changes:
  - Closed the executable-permission build error as fixed.
  - Verified the published image is accessible from the Linux VM.
  - Captured the current hbbs/hbbr image, commands, host networking, restart
    policy, persistent mount, listeners, disk use, and security-update state.
  - Recorded security observations without changing VM configuration.

Reasons:
  - Establish a verified baseline and rollback reference before replacing the
    production containers.

Validation:
  - GitHub Actions run 27046280606 completed successfully.
  - Published hbbs and hbbr image smoke tests passed.
  - VM image manifest access succeeded.
  - Current containers remain healthy and unchanged.
  - UFW is inactive, SSH password authentication is enabled, unattended
    upgrades are active, and no network or security setting was modified.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:00:00+02:00
Title: Separate server operational records from client records

Changed files:
  - AGENTS.md
  - logging/BuildErrorServer_log.txt
  - logging/revisions.txt
  - logging/revisions_server.txt

Changes:
  - Created dedicated server revision and build-error logs in the main project.
  - Migrated concise summaries of all custom server changes and confirmed
    GitHub Actions failures completed so far.
  - Reserved `logging/InforiaConnectServer_Tehnicni_pregled.docx` for creation
    after deployment and end-to-end production validation.

Reasons:
  - Keep Windows client history and Linux server history independently
    readable and auditable.

Validation:
  - Confirmed the migrated entries match the InforiaConnect-server repository
    history and GitHub Actions results.

-------------------------------------------------------------------------------
